__pycache__/
*.pyc
.DS_Store
training/data/
*.pt
*.safetensors
script/

# harness_pretrain standalone runs (uv run train.py): local checkpoints,
# metrics, and chuk-train data: staging -- generated wherever it's run from,
# never meant to be committed.
ckpt/
metrics.jsonl
training/harness_pretrain/data/
# the local 16M-token pretrain: checkpoints, metrics and its stdout log
run_pretrain/

# generated by training/export_repl_checkpoint.py from a run_pretrain/ checkpoint
# -- weights plus the config.json that describes them. Reproducible, so not
# committed; *.pt was already ignored but config.json was not.
model_v11/
