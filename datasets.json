{
  "_comment": [
    "What this repo puts in the chuk-datasets catalog (chuk-datasets.fly.dev).",
    "",
    "Register with the shared tool, which lives with the server that owns the",
    "identity algorithm so there is exactly one implementation of it:",
    "",
    "  uv run <chuk-datasets-server>/jobs/register-files/register.py \\",
    "      register datasets.json --dry-run     # no credential needed",
    "  uv run ... register datasets.json        # needs CHUK_DATASETS_API_KEY",
    "",
    "IMPORTANT, and different from v-tokenizers' entry: `root` here is",
    "gitignored. training/data/ is generated by build_mathonly_corpus.py, not",
    "committed -- 29MB of regenerable JSONL. So this repo deliberately does NOT",
    "run `register.py verify` in CI: a fresh checkout has no files to hash and",
    "the job would fail for the wrong reason. Verification here happens at",
    "registration time and at train time (the corpus guard in train_mathonly.py",
    "refuses ids outside the model's vocabulary), not in CI.",
    "",
    "Classes are the spec's enum: pretrain-stream | eval-set | probe-corpus |",
    "midtrain-corpus | raw-source. This is Act 3's midtrain mixture, so",
    "midtrain-corpus -- matching cells/cn7-midtrain-corpus, which is the same",
    "shape minus the cell species."
  ],

  "server": "https://chuk-datasets.fly.dev",

  "datasets": [
    {
      "name": "tiny-model/mathonly-midtrain",
      "class": "midtrain-corpus",
      "description": "Act 3's maths-only (zero cell content) midtrain mixture for TinyModel v11: Tier A arithmetic drill at 55% plus TinyStories replay at 45%, tokenized with the published v11 build (vocab 71,260). The clean S1+S4 control arm CN-7's write-up asks for.",
      "version_id": "v1",
      "root": "training/data",
      "envelope": { "format": "jsonl" },
      "provenance": {
        "source": "derived",
        "repo": "chrishayuk/tinystories-train-video",
        "path": "training/build_mathonly_corpus.py",
        "generator": {
          "command": "uv run training/build_mathonly_corpus.py --drill 90000 --seed 90",
          "seed": 90,
          "deterministic": "drill only",
          "note": "CORRECTED 2026-07-25. The drill half IS deterministic -- pure-python RNG at seed 90, bit-identical across machines (1,859,026 tokens on both an M3 and a Colab T4). The REPLAY half is NOT: it streams TinyStories through datasets' shuffle(seed=90, buffer_size=10000), whose order depends on shard arrival, so a rebuild yields a slightly different replay set every time (1,521,190 vs 1,521,059 tokens; 96,889 vs 96,890 rows across two builds of the same command). Because train_mathonly.py takes its validation slice as the last 10% of replay rows, that difference changes the VALIDATION SET -- 663 rows on one build, 710 on another -- so replay-NLL numbers from separate builds are not comparable at all. This is precisely why the corpus is registered: fetching these bytes is the only way to get the reproducibility the seed was assumed to provide."
        },
        "composition": {
          "drill_rows": 90000,
          "drill_tokens": 1859026,
          "replay_tokens": 1521190,
          "replay_frac": 0.45,
          "total_rows": 96889,
          "operations": ["add", "sub", "mul", "mod", "cmp", "parity", "min3", "succ"],
          "note": "add and sub are drawn twice as often as the rest. Operand ranges are CN-7's s1_item verbatim (add/sub 0-99, mul half times-tables half 1-digit x 2-digit, mod 0-99 mod 2-12, cmp/parity/min3 0-999) -- this file's S1 is a copy of that generator, so the two arms differ by the cell species and nothing else."
        },
        "surfaces": {
          "canonical_per_op": 4,
          "narrative_per_op": "3-4",
          "commutative_orders_taught": ["add", "mul"],
          "note": "Each fact appears across several phrasings with the answer sometimes leading and the operands sometimes reversed. Not decoration: with one template and fixed slots the cheapest thing to memorise is the STRING, which would make the held-out probe below meaningless."
        },
        "held_out": {
          "file": "mathonly_held_out.json",
          "op": "add",
          "pairs": 250,
          "symmetric": true,
          "note": "Withheld from every surface in both orders, so a held-out fact cannot leak via commutativity. Included in this dataset's identity on purpose -- a corpus is not fully described without knowing what was deliberately kept out of it. The probe (training/heldout_probe_mathonly.py) additionally scores the ~982 facts that go unseen naturally."
        },
        "termination": {
          "eos_on_drill_rows": true,
          "eos_on_replay_rows": false,
          "note": "The registered R1 remediation ('add S1 EOS supervision'). Without it, free-running generation ran on into a template-echo mode and P-b read 0.111 against 0.91/0.97 teacher-forced on the same checkpoint. Replay rows are whole TinyStories and stay unterminated, so story generation is unchanged."
        },
        "tokenizer": {
          "name": "v11",
          "vocab_size": 71260,
          "tokenizer_sha": "10dd51100331ab503115db23eee7e8dc3e360e3aed697c8a2e1b12b8f46031ae",
          "note": "The published build (HF chrishayuk/v11-tokenizer, PyPI v11-tokenizer, crates.io v11-core). Rows are pre-tokenized, so nothing in the bytes records which tokenizer produced them -- this field is how that is recoverable, and train_mathonly.py refuses any corpus whose ids fall outside the model's vocabulary."
        },
        "replay_source": {
          "hub_dataset": "roneneldan/TinyStories",
          "hub_revision": "f54c09fd23315a6f9c86f9dc80f725de7d8f9c64",
          "note": "Same pinned revision the base model was pretrained on, so replay is genuinely replay rather than new data."
        },
        "base_model": {
          "hf": "chrishayuk/v11-tinystories-115m-base",
          "model_sha256": "1841e0581574629716b646dacd4e70feaca153a8adc5ecb0b77e0e2ebdf78d9c",
          "note": "The checkpoint this corpus mid-trains. Recorded here because the pair is what a result is about; neither alone reproduces anything."
        },
        "related": {
          "cn7_experiment": "cn-7-r1-numeracy-midtrain",
          "cn7_corpus": "cells/cn7-midtrain-corpus",
          "note": "CN-7 trained S1+S2+S3+S4 together. This is S1+S4 only -- the maths-only control its next_action asks for. Its S1 is 90,000 rows / 1,662,749 tokens against this one's 90,000 / 1,859,026; the difference is the added surfaces and EOS."
        }
      }
    }
  ]
}
